{
  "name": "workers-axiom-example",
  "version": "0.3.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy --var VERSION:0.3.0",
    "deploy-auto-version": "wrangler deploy --var VERSION:$(cat package.json|jq -r '.version')-$(git log -1 --pretty=format:%h)",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230419.0",
    "typescript": "^5.0.4",
    "wrangler": "^3.0.0"
  }
}
